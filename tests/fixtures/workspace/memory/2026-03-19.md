## 10:00

- Switched test runner from Jest to Bun
- Bun test is 3x faster for this project

## 11:30

- Alex prefers error messages to be actionable, not just descriptive
- Example: "File not found: config.toml — run `init` to create it" instead of "File not found"
